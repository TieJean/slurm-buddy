#!/usr/bin/env bash
# slurm-buddy entry point. Runs the package with no install needed.
REPO_ROOT="$(cd "$(dirname "$(readlink -f "${BASH_SOURCE[0]}")")/.." && pwd)"
exec env PYTHONPATH="${REPO_ROOT}${PYTHONPATH:+:$PYTHONPATH}" python3 -m slurmbuddy "$@"
